Focus on semantic HTML elements, heading structure, form labeling, image alt text, unnecessary wrapper elements, and readable document structure.

Point out invalid or weak structure.
Suggest more semantic tags when helpful.
